
# =========================================================================
# AL-E System Environment Variables Example
# =========================================================================
# This file lists all environment variables required for the AL-E system.
# Copy this file to `.env.local` and fill in your actual credentials.
#
# IMPORTANT: For production deployments, ensure these variables are set
# securely in your hosting environment, NOT committed to version control.
#
# =========================================================================


# -------------------------------------------------------------------------
# Supabase Configuration
# -------------------------------------------------------------------------
# Supabase is used for user authentication, database, and file storage.
#
# How to get these:
# 1. Create a Supabase project at https://app.supabase.com/
# 2. Navigate to "Project Settings" -> "API".
# 3. Copy your "Project URL" and "anon (public)" key.
VITE_SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_ANON_KEY"


# -------------------------------------------------------------------------
# Google Calendar API (bidirectional sync)
# -------------------------------------------------------------------------
# Used for fetching and syncing calendar events.
#
# How to get these:
# 1. Go to Google Cloud Console (console.cloud.google.com).
# 2. Create a new project or select an existing one.
# 3. Enable the "Google Calendar API".
# 4. Go to "APIs & Services" -> "Credentials".
# 5. Create "OAuth 2.0 Client IDs" for "Web application".
# 6. Configure authorized redirect URIs (e.g., http://localhost:3000/auth/callback).
# 7. The VITE_GOOGLE_CLIENT_ID will be your "Client ID".
# 8. Note: Full OAuth flow (backend handling tokens) is required for production.
VITE_GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"


# -------------------------------------------------------------------------
# Outlook Calendar API (sync)
# -------------------------------------------------------------------------
# Used for syncing calendar events with Microsoft Outlook/Exchange.
#
# How to get these:
# 1. Go to Azure Portal (portal.azure.com).
# 2. Register a new application in "Azure Active Directory" -> "App registrations".
# 3. Grant appropriate API permissions (e.g., `Calendars.ReadWrite` for Microsoft Graph).
# 4. Configure authentication for "Web" and add redirect URIs.
# 5. The VITE_OUTLOOK_CLIENT_ID will be your "Application (client) ID".
# 6. Note: Full OAuth flow (backend handling tokens) is required for production.
VITE_OUTLOOK_CLIENT_ID="YOUR_OUTLOOK_CLIENT_ID"


# -------------------------------------------------------------------------
# Zoom API (create meeting rooms)
# -------------------------------------------------------------------------
# Used for creating and managing Zoom meeting rooms directly from the app.
#
# How to get these:
# 1. Go to Zoom App Marketplace (marketplace.zoom.us).
# 2. Create a "Server-to-Server OAuth" app (recommended for secure server-side apps).
# 3. Get your "Account ID", "Client ID", and "Client Secret".
# 4. IMPORTANT: The Client Secret should NEVER be exposed on the frontend.
#    Zoom API calls should be proxied through a secure backend (e.g., Supabase Edge Functions).
#    If using a backend, you'd configure VITE_ZOOM_CLIENT_SECRET there.
VITE_ZOOM_API_KEY="YOUR_ZOOM_API_KEY" # This might be a Client ID if making direct calls, but backend proxy is safer.


# -------------------------------------------------------------------------
# Google Meet API (create meeting rooms)
# -------------------------------------------------------------------------
# Used for creating Google Meet meeting rooms.
#
# How to get these:
# 1. Similar to Google Calendar API setup in Google Cloud Console.
# 2. Enable "Google Meet API" in your project.
# 3. The same VITE_GOOGLE_CLIENT_ID from Google Calendar can often be reused,
#    but ensure the appropriate scopes for Google Meet are requested during OAuth.
# VITE_GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID" # (Already defined above if using Google Calendar)


# -------------------------------------------------------------------------
# Twilio API (SMS reminders)
# -------------------------------------------------------------------------
# Used for sending SMS reminders and notifications.
#
# How to get these:
# 1. Create a Twilio account (www.twilio.com).
# 2. Get your "Account SID" and "Auth Token".
# 3. Get a Twilio phone number.
# 4. IMPORTANT: These credentials (especially Auth Token) are highly sensitive
#    and MUST NEVER be exposed on the frontend.
#    All calls to Twilio should be proxied through a secure backend (e.g., Supabase Edge Function).
#    These variables should typically be set as secrets in your backend environment.
VITE_TWILIO_ACCOUNT_SID="YOUR_TWILIO_ACCOUNT_SID"
VITE_TWILIO_AUTH_TOKEN="YOUR_TWILIO_AUTH_TOKEN" # Backend only
VITE_TWILIO_PHONE_NUMBER="YOUR_TWILIO_PHONE_NUMBER"


# -------------------------------------------------------------------------
# WhatsApp Cloud API (send messages)
# -------------------------------------------------------------------------
# Used for sending structured messages via WhatsApp.
#
# How to get these:
# 1. Set up a Meta for Developers account (developers.facebook.com).
# 2. Create a WhatsApp Business Account and connect it to a WhatsApp Business App.
# 3. Obtain a permanent access token and your "Phone Number ID".
# 4. IMPORTANT: The access token is highly sensitive and MUST be handled by a secure backend.
#    Do NOT expose this on the frontend.
VITE_WHATSAPP_TOKEN="YOUR_WHATSAPP_ACCESS_TOKEN" # Backend only
VITE_WHATSAPP_PHONE_ID="YOUR_WHATSAPP_PHONE_NUMBER_ID" # Backend only


# -------------------------------------------------------------------------
# Google Maps API (Waze/Google Maps functionality, route calculation)
# -------------------------------------------------------------------------
# Used for embedding maps, calculating routes, and displaying location-based data.
#
# How to get these:
# 1. Go to Google Cloud Console (console.cloud.google.com).
# 2. Enable "Maps JavaScript API" and "Directions API" (and any other Maps-related APIs you need).
# 3. Go to "APIs & Services" -> "Credentials".
# 4. Create an API Key.
# 5. IMPORTANT: Restrict this API key to your domain (HTTP referrers) to prevent unauthorized use.
VITE_GOOGLE_MAPS_API_KEY="YOUR_GOOGLE_MAPS_API_KEY"


# -------------------------------------------------------------------------
# OpenAI API (AI contextual features)
# -------------------------------------------------------------------------
# Used for integrating advanced AI capabilities like contextual summaries,
# text generation, risk assessment, etc.
#
# How to get these:
# 1. Create an account and API key at platform.openai.com.
# 2. IMPORTANT: The OpenAI API key is highly sensitive and MUST be handled by a secure backend.
#    Do NOT expose this on the frontend.
VITE_OPENAI_API_KEY="YOUR_OPENAI_API_KEY" # Backend only


# -------------------------------------------------------------------------
# Text-to-Speech (TTS) & Speech-to-Text (STT) APIs
# -------------------------------------------------------------------------
# These integrations primarily rely on the browser's native Web Speech API.
# No external API keys are typically required for basic browser functionality.
#
# For advanced TTS/STT (e.g., higher accuracy, more voices), you might
# consider services like Google Cloud Speech-to-Text or Azure Speech Services,
# which would require their own API keys and backend integration.
# Currently, no VITE_ variables are needed for the browser-native implementations.
